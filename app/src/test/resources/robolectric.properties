# =================================================================================
# FILE: ./app/src/test/resources/robolectric.properties
# REASON: FIX(test) - This configuration file explicitly tells the Robolectric
# test runner where to find the native SQLCipher libraries (.so files). This
# makes the libraries available to the test environment's class loader,
# resolving the UnsatisfiedLinkError.
# =================================================================================
nativeLibraryPath = ../../../build/intermediates/stripped_native_libs/debug/out/lib
